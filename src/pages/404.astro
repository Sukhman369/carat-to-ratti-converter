---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
---

<Layout
  title="404 · The Cosmic Gemstone Vault | Ratti to Carat"
  description="You've discovered the celestial gemstone vault. Calibrate the cosmic balance scale, balance sacred Navratna gems, or return to the live converter."
  canonicalUrl="https://rattitocarat.com/404"
>
  <Navbar />

  <main class="relative min-h-[85vh] flex flex-col items-center justify-center px-4 sm:px-6 py-12 overflow-hidden">
    
    <!-- Ambient Celestial Glow Orbs -->
    <div class="absolute top-1/4 left-1/2 -translate-x-1/2 w-96 h-96 bg-gradient-to-tr from-amber-300/20 via-orange-300/10 to-amber-500/20 rounded-full blur-3xl pointer-events-none -z-10"></div>

    <div class="max-w-2xl w-full text-center space-y-8">
      
      <!-- Top Status Header -->
      <div class="space-y-3">
        <div class="inline-flex items-center gap-2 px-3.5 py-1 rounded-full bg-amber-50 text-amber-800 text-xs font-mono font-bold uppercase tracking-widest border border-amber-200 shadow-xs">
          <span>✨ 404 · Uncharted Celestial Coordinates</span>
        </div>
        <h1 class="text-3xl sm:text-4xl md:text-5xl font-extrabold tracking-tight text-slate-900 font-display">
          Lost in the Cosmic Gem Vault
        </h1>
        <p class="text-xs sm:text-sm text-slate-600 max-w-md mx-auto leading-relaxed">
          The page you requested has dissolved into stardust. But while you're here, tap the sacred Navratna gems to balance the ancient celestial scale!
        </p>
      </div>

      <!-- Smart URL Intent Detector (Appears if user mistyped a weight URL) -->
      <div id="url-intent-box" class="hidden p-4 rounded-2xl bg-amber-50/90 border border-amber-300/80 shadow-xs text-center space-y-2 animate-in fade-in zoom-in-95 duration-300">
        <div class="flex items-center justify-center gap-2 text-xs font-bold text-amber-900">
          <span>🔍</span>
          <span>We detected a weight in your link!</span>
        </div>
        <p class="text-xs text-slate-700" id="url-intent-text"></p>
        <a
          id="url-intent-link"
          href="/"
          class="inline-flex items-center gap-1.5 px-4 py-1.5 rounded-lg bg-amber-600 hover:bg-amber-700 text-white font-bold text-xs shadow-xs transition-all"
        >
          <span>Calculate This Weight Now →</span>
        </a>
      </div>

      <!-- Interactive Celestial Balance Scale (The Viral Interactive Core) -->
      <div class="rounded-3xl atelier-card-elevated p-6 sm:p-8 border border-slate-200 shadow-xl space-y-6 glow-gold-sm text-center">
        
        <!-- Live Scale Readout -->
        <div class="flex flex-wrap items-center justify-between gap-3 pb-4 border-b border-slate-100 text-xs">
          <div class="flex items-center gap-2">
            <span class="w-2 h-2 rounded-full bg-emerald-500 animate-ping"></span>
            <span class="font-bold text-slate-800">Cosmic Balance Scale</span>
          </div>
          <div class="font-mono font-bold text-amber-800 bg-amber-50 px-3 py-1 rounded-full border border-amber-200">
            Total Mass: <span id="scale-total-ratti">0.00</span> Rt (<span id="scale-total-carats">0.00</span> Ct)
          </div>
        </div>

        <!-- The Animated Physical Balance Scale -->
        <div class="relative h-44 sm:h-52 w-full flex items-center justify-center overflow-hidden select-none">
          
          <!-- Central Pivot Stand -->
          <div class="absolute bottom-4 left-1/2 -translate-x-1/2 w-4 h-28 bg-gradient-to-t from-slate-400 to-amber-500 rounded-t-sm shadow-md"></div>
          <div class="absolute bottom-2 left-1/2 -translate-x-1/2 w-16 h-3 bg-slate-300 rounded-full border border-slate-400"></div>
          <div class="absolute top-12 left-1/2 -translate-x-1/2 w-5 h-5 rounded-full bg-amber-500 border-2 border-white shadow-sm z-20"></div>

          <!-- Rotating Tilting Beam -->
          <div
            id="scale-beam"
            class="absolute top-14 left-1/2 -translate-x-1/2 w-64 sm:w-80 h-2 bg-gradient-to-r from-amber-400 via-amber-600 to-amber-400 rounded-full shadow-md transition-transform duration-500 ease-out origin-center z-10"
            style="transform: translateX(-50%) rotate(0deg);"
          >
            <!-- Left Pan Hanging Rope & Tray -->
            <div class="absolute -left-1 top-2 flex flex-col items-center">
              <div class="w-0.5 h-16 bg-slate-400/80"></div>
              <div class="w-16 h-4 bg-gradient-to-b from-amber-200 to-amber-400 rounded-b-xl border border-amber-500 shadow-sm flex items-center justify-center">
                <span id="left-pan-count" class="text-[9px] font-mono font-bold text-amber-900">0</span>
              </div>
            </div>

            <!-- Right Pan Hanging Rope & Tray -->
            <div class="absolute -right-1 top-2 flex flex-col items-center">
              <div class="w-0.5 h-16 bg-slate-400/80"></div>
              <div class="w-16 h-4 bg-gradient-to-b from-amber-200 to-amber-400 rounded-b-xl border border-amber-500 shadow-sm flex items-center justify-center">
                <span id="right-pan-count" class="text-[9px] font-mono font-bold text-amber-900">0</span>
              </div>
            </div>
          </div>

          <!-- Active Oracle Message -->
          <div class="absolute bottom-0 inset-x-0 text-center">
            <p id="oracle-message" class="text-xs font-semibold text-slate-700 italic">
              "Click any sacred gemstone below to load the cosmic scale..."
            </p>
          </div>
        </div>

        <!-- 9 Tap-to-Drop Navratna Gemstone Buttons -->
        <div class="space-y-2 pt-2 border-t border-slate-100">
          <span class="text-[11px] font-bold uppercase tracking-wider text-slate-500 block">
            Tap Gems to Balance · Hear the Chime 🔔
          </span>
          
          <div class="grid grid-cols-3 sm:grid-cols-5 md:grid-cols-9 gap-1.5" id="gems-dock">
            <button type="button" class="gem-btn p-2 rounded-xl bg-slate-50 hover:bg-amber-100/60 border border-slate-200 transition-all hover:scale-105 active:scale-90 flex flex-col items-center gap-0.5" data-gem="Manik (Ruby)" data-ratti="4.25" data-side="left" data-freq="528">
              <span class="text-base">☀️</span>
              <span class="text-[10px] font-bold text-slate-800">Manik</span>
              <span class="text-[9px] font-mono text-slate-500">4.25 Rt</span>
            </button>

            <button type="button" class="gem-btn p-2 rounded-xl bg-slate-50 hover:bg-amber-100/60 border border-slate-200 transition-all hover:scale-105 active:scale-90 flex flex-col items-center gap-0.5" data-gem="Moti (Pearl)" data-ratti="5.25" data-side="left" data-freq="587">
              <span class="text-base">🌕</span>
              <span class="text-[10px] font-bold text-slate-800">Moti</span>
              <span class="text-[9px] font-mono text-slate-500">5.25 Rt</span>
            </button>

            <button type="button" class="gem-btn p-2 rounded-xl bg-slate-50 hover:bg-amber-100/60 border border-slate-200 transition-all hover:scale-105 active:scale-90 flex flex-col items-center gap-0.5" data-gem="Pukhraj (Yellow Sapphire)" data-ratti="5.25" data-side="left" data-freq="659">
              <span class="text-base">🌟</span>
              <span class="text-[10px] font-bold text-slate-800">Pukhraj</span>
              <span class="text-[9px] font-mono text-slate-500">5.25 Rt</span>
            </button>

            <button type="button" class="gem-btn p-2 rounded-xl bg-slate-50 hover:bg-amber-100/60 border border-slate-200 transition-all hover:scale-105 active:scale-90 flex flex-col items-center gap-0.5" data-gem="Panna (Emerald)" data-ratti="3.25" data-side="left" data-freq="698">
              <span class="text-base">🌿</span>
              <span class="text-[10px] font-bold text-slate-800">Panna</span>
              <span class="text-[9px] font-mono text-slate-500">3.25 Rt</span>
            </button>

            <button type="button" class="gem-btn p-2 rounded-xl bg-slate-50 hover:bg-amber-100/60 border border-slate-200 transition-all hover:scale-105 active:scale-90 flex flex-col items-center gap-0.5" data-gem="Heera (Diamond)" data-ratti="1.25" data-side="right" data-freq="784">
              <span class="text-base">💎</span>
              <span class="text-[10px] font-bold text-slate-800">Heera</span>
              <span class="text-[9px] font-mono text-slate-500">1.25 Rt</span>
            </button>

            <button type="button" class="gem-btn p-2 rounded-xl bg-slate-50 hover:bg-amber-100/60 border border-slate-200 transition-all hover:scale-105 active:scale-90 flex flex-col items-center gap-0.5" data-gem="Neelam (Blue Sapphire)" data-ratti="4.25" data-side="right" data-freq="880">
              <span class="text-base">🪐</span>
              <span class="text-[10px] font-bold text-slate-800">Neelam</span>
              <span class="text-[9px] font-mono text-slate-500">4.25 Rt</span>
            </button>

            <button type="button" class="gem-btn p-2 rounded-xl bg-slate-50 hover:bg-amber-100/60 border border-slate-200 transition-all hover:scale-105 active:scale-90 flex flex-col items-center gap-0.5" data-gem="Moonga (Coral)" data-ratti="6.25" data-side="right" data-freq="987">
              <span class="text-base">🔥</span>
              <span class="text-[10px] font-bold text-slate-800">Moonga</span>
              <span class="text-[9px] font-mono text-slate-500">6.25 Rt</span>
            </button>

            <button type="button" class="gem-btn p-2 rounded-xl bg-slate-50 hover:bg-amber-100/60 border border-slate-200 transition-all hover:scale-105 active:scale-90 flex flex-col items-center gap-0.5" data-gem="Gomed (Hessonite)" data-ratti="7.25" data-side="right" data-freq="1046">
              <span class="text-base">🌪️</span>
              <span class="text-[10px] font-bold text-slate-800">Gomed</span>
              <span class="text-[9px] font-mono text-slate-500">7.25 Rt</span>
            </button>

            <button type="button" class="gem-btn p-2 rounded-xl bg-slate-50 hover:bg-amber-100/60 border border-slate-200 transition-all hover:scale-105 active:scale-90 flex flex-col items-center gap-0.5" data-gem="Lehsuniya (Cat's Eye)" data-ratti="5.25" data-side="right" data-freq="1174">
              <span class="text-base">👁️</span>
              <span class="text-[10px] font-bold text-slate-800">Lehsuniya</span>
              <span class="text-[9px] font-mono text-slate-500">5.25 Rt</span>
            </button>
          </div>
        </div>

        <!-- Scale Controls -->
        <div class="flex items-center justify-center gap-3 pt-2">
          <button
            type="button"
            id="btn-reset-scale"
            class="text-xs font-semibold text-slate-500 hover:text-slate-900 underline transition-colors"
          >
            Reset Scale
          </button>
          <span>·</span>
          <button
            type="button"
            id="btn-share-cosmic"
            class="text-xs font-semibold text-amber-700 hover:text-amber-800 flex items-center gap-1 transition-colors"
          >
            <span>✨ Share Cosmic Score</span>
          </button>
        </div>

      </div>

      <!-- Teleport Back Actions -->
      <div class="flex flex-col sm:flex-row items-center justify-center gap-3 pt-2">
        <a
          href="/"
          class="w-full sm:w-auto h-12 px-7 rounded-xl bg-slate-900 hover:bg-slate-800 text-white font-bold text-sm shadow-md flex items-center justify-center gap-2.5 transition-all hover:scale-105 active:scale-95"
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-amber-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
            <polyline points="9 22 9 12 15 12 15 22"/>
          </svg>
          <span>Teleport to Master Calculator</span>
        </a>

        <a
          href="/body-weight-calculator"
          class="w-full sm:w-auto h-12 px-5 rounded-xl bg-white hover:bg-slate-50 text-slate-800 font-semibold text-sm border border-slate-200 shadow-xs flex items-center justify-center gap-2 transition-all hover:scale-105 active:scale-95"
        >
          <span>Body Weight Dosage</span>
        </a>
      </div>

    </div>
  </main>

  <Footer />
</Layout>

<!-- Web Audio Chimes & Balance Physics Engine -->
<script>
  let audioCtx: AudioContext | null = null;

  function playChime(freq: number) {
    try {
      if (!audioCtx) {
        audioCtx = new (window.AudioContext || (window as any).webkitAudioContext)();
      }
      if (audioCtx.state === 'suspended') {
        audioCtx.resume();
      }
      const osc = audioCtx.createOscillator();
      const gain = audioCtx.createGain();
      osc.type = 'sine';
      osc.frequency.setValueAtTime(freq, audioCtx.currentTime);
      gain.gain.setValueAtTime(0.15, audioCtx.currentTime);
      gain.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + 0.6);
      osc.connect(gain);
      gain.connect(audioCtx.destination);
      osc.start();
      osc.stop(audioCtx.currentTime + 0.6);
    } catch (e) {
      // Audio not permitted or supported
    }
  }

  let leftWeight = 0;
  let rightWeight = 0;
  let leftCount = 0;
  let rightCount = 0;

  const beam = document.getElementById('scale-beam');
  const totalRattiEl = document.getElementById('scale-total-ratti');
  const totalCaratsEl = document.getElementById('scale-total-carats');
  const leftCountEl = document.getElementById('left-pan-count');
  const rightCountEl = document.getElementById('right-pan-count');
  const oracleMsg = document.getElementById('oracle-message');
  const btnReset = document.getElementById('btn-reset-scale');
  const btnShare = document.getElementById('btn-share-cosmic');
  const gemButtons = document.querySelectorAll<HTMLButtonElement>('.gem-btn');

  const FORTUNES = [
    "Jupiter radiates benevolence! Sawa weight harmony achieved ✨",
    "Saturn demands disciplined focus. The cosmic balance shifts 🪐",
    "Venus adds luminous brilliance to your destiny 💎",
    "The Sun bolsters vitality and authority! ☀️",
    "Mercury sharpens your intellect with emerald precision 🌿"
  ];

  function updateScale() {
    const totalRatti = leftWeight + rightWeight;
    const totalCarats = totalRatti * 0.91;

    if (totalRattiEl) totalRattiEl.innerText = totalRatti.toFixed(2);
    if (totalCaratsEl) totalCaratsEl.innerText = totalCarats.toFixed(2);
    if (leftCountEl) leftCountEl.innerText = `${leftWeight.toFixed(1)} Rt`;
    if (rightCountEl) rightCountEl.innerText = `${rightWeight.toFixed(1)} Rt`;

    // Calculate rotation angle (max ±14 deg)
    const diff = rightWeight - leftWeight;
    const angle = Math.max(-14, Math.min(14, diff * 2.5));

    if (beam) {
      beam.style.transform = `translateX(-50%) rotate(${angle}deg)`;
    }

    if (oracleMsg) {
      if (Math.abs(diff) < 0.1 && totalRatti > 0) {
        oracleMsg.innerText = "✨ Perfect Cosmic Equilibrium Achieved! Astrological forces aligned.";
      } else {
        const randFortune = FORTUNES[Math.floor(Math.random() * FORTUNES.length)];
        oracleMsg.innerText = randFortune;
      }
    }
  }

  gemButtons.forEach((btn) => {
    btn.addEventListener('click', () => {
      const ratti = parseFloat(btn.getAttribute('data-ratti') || '1');
      const side = btn.getAttribute('data-side') || 'left';
      const freq = parseFloat(btn.getAttribute('data-freq') || '528');

      playChime(freq);

      if (side === 'left') {
        leftWeight += ratti;
        leftCount++;
      } else {
        rightWeight += ratti;
        rightCount++;
      }

      updateScale();
    });
  });

  btnReset?.addEventListener('click', () => {
    leftWeight = 0;
    rightWeight = 0;
    leftCount = 0;
    rightCount = 0;
    updateScale();
    if (oracleMsg) oracleMsg.innerText = "Scale recalibrated. Tap any gemstone to begin.";
  });

  btnShare?.addEventListener('click', () => {
    const totalRatti = (leftWeight + rightWeight).toFixed(2);
    const text = `I got lost in the 404 Vault on rattitocarat.com and balanced ${totalRatti} Ratti of celestial Navratna gems! ✨ Check it out: https://rattitocarat.com/404`;
    if (navigator.clipboard) {
      navigator.clipboard.writeText(text).then(() => {
        alert('Copied your Cosmic Score to clipboard! Share it with friends ✨');
      });
    }
  });

  // Intelligent URL Intent Detection (e.g. if URL is /5.25 or /carat-to-ratti/7)
  const path = window.location.pathname;
  const numMatch = path.match(/\b\d+(\.\d+)?\b/);
  if (numMatch) {
    const num = parseFloat(numMatch[0]);
    if (!isNaN(num) && num > 0 && num <= 100) {
      const intentBox = document.getElementById('url-intent-box');
      const intentText = document.getElementById('url-intent-text');
      const intentLink = document.getElementById('url-intent-link') as HTMLAnchorElement;

      if (intentBox && intentText && intentLink) {
        intentText.innerText = `You entered "${path}". Did you want to convert ${num} Ratti (${(num * 0.91).toFixed(2)} Carats)?`;
        intentLink.href = `/?weight=${num}`;
        intentBox.classList.remove('hidden');
      }
    }
  }
</script>
